# Automated_Workplace
automated workplace of ultrasound examination room

1) change directory to frontend, install modules for frontend with:  
  npm install
2) change directory to api, install modules for api with:
  python -m venv .venv
3) change directory to root, build and run containers with:
  docker compose up -d
